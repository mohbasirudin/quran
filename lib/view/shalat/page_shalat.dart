import 'package:flutter/material.dart';

class PageShalat extends StatelessWidget {
  const PageShalat({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
